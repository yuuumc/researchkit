# ResearchKit OS v2.3.0 — i18n 完整国际化

> **发布日期**：2026-07-22
> **类型**：Minor Release
> **5 天实施计划**：D36-D40（详见 `docs/v2.3-i18n-plan.md`）

---

## 核心亮点

### 4 层语言分离架构

ResearchKit OS v2.3.0 引入清晰的语言职责分离，覆盖 4 种语言维度：

| 层级 | 名称 | 作用 | 可选值 |
|---|---|---|---|
| 1 | **Application Language** | UI 文案 / Help / Tooltip / Preset label | `auto / zh-CN / en-US / ja-JP` |
| 2 | **Output Language** | KC 输出语言 | `auto`（跟随源语言）或显式 `zh-CN / en-US / ja-JP` |
| 3 | **Auto Translate** | Explain / Chat / Compare 回复语言 | `On / Off`（开启时跟随 Application Language） |
| 4 | **Prompt Language** | LLM 内部 prompt 语言 | 锁死 `en-US`（效果最佳，不可改） |

**技术栈**：自建零依赖 `t(key, params, locale)` 系统（< 1KB），6 个 namespace（`home / agent / common / settings / preset / export`），无重型 i18next / react-intl 依赖。

---

## 5 天实施计划（D36-D40）

### D36 — i18n 基础设施

- `lib/i18n.ts` — 核心 `t()` 函数，支持 `{param}` 占位符替换
- `lib/locale-types.ts` — `AppLocale` / `ResolvedLocale` 类型
- `components/I18nProvider.tsx` — `useI18n()` hook 返回 `{ appLocale, resolvedLocale, t, setLocale }`
- SSR 行为：服务端渲染 `'auto'` → `'en-US'`，hydration 后切换到 cookie 中的 locale

### D37 — Settings UI 全面国际化（PR #30）

- 5 个 Settings Tab 标签走 i18n
- `components/settings/tabs/GeneralTab.tsx` — 4 层语言设置选择器
- `ProviderTab / PromptTab / CostTab / AboutTab` — 全部本地化
- 新增 60+ locale key 到 `locales/{zh-CN,en-US}/settings.json`

### D38 — 主页 + Agent 模块国际化（PR #31）

**主页 + KC 字段标签**：
- `app/page.tsx` — 全部 UI 文案走 `t()`（`home.*` namespace）
- `lib/ui-labels.ts` — 新增 `getKcFieldLabels(appLocale)`，KC 字段标签跟随 Application Language（而非 Output Language）

**4 个 Agent 组件 i18n 化**：
- `AgentTimeline.tsx`
- `SmartSuggestionBanner.tsx` — `fmtRelativeTime(ts, t)` 接收 t 函数
- `ChatWithKC.tsx` — `generateSuggestions(kc, t)` 重构为接收 t 函数
- `ExplainKC.tsx` — `AUDIENCE_OPTIONS` 改为 `localeKey` + `audienceLabel()` 辅助函数

**其他组件**：
- `CompareTab.tsx` + `PluginPanel.tsx` 全部 i18n 化

**Locales**：
- `locales/{zh-CN,en-US}/home.json` — +95 行
- `locales/{zh-CN,en-US}/agent.json` — +179 行

### D39 — Auto Translate + 智能检测（PR #32）

**前端 — 输入语言检测**：
- `lib/detect-language.ts` — `detectInputLanguage()` 复用 `lib/locale.ts` 的 `detectLocale`（Unicode 范围统计），返回 `{ detected, sampleSize, counts, confidence }`
- `getLanguageSuggestion()` — 智能建议触发条件：
  - `outputLocale === 'auto'`（用户未显式选 Output）
  - `detected !== resolveAppLocale(appLocale)`（源语言与 UI 语言不同）
  - `detected !== 'other'`（无法判断时不建议）
- `components/LanguageDetectBanner.tsx` — 显示检测结果 + 详情折叠 + 一键切换建议
  - 防抖 300ms，文本 < 20 字符不显示，"忽略" 按本次会话记忆
- `app/page.tsx` 集成 Banner（text/url/batch 模式）

**后端 — Auto Translate 指令**：
- `lib/server-user-preferences.ts` 新增 `buildAutoTranslateDirective()`
  - `autoTranslate=false` → 返回空字符串（原行为）
  - `autoTranslate=true` → 追加 `Please respond in {appLocale}.` 指令到 system prompt 末尾
- 3 个路由追加 directive：
  - `app/api/research/explain-kc/route.ts`
  - `app/api/research/chat-kc/route.ts`
  - `app/api/research/compare-papers/route.ts`

### D40 — 回归测试 i18n 扩展

- `scripts/regression-test.ts` 扩展：
  - 每篇 fixture 跑两次（`appLocale = outputLocale = zh-CN / en-US`）
  - 通过 cookie 注入 `researchkit-user-preferences`（base64 JSON）
  - `validateKc` 校验按 `expectedOutputLocale` 而非 `fixture.locale`
  - 报告新增 `AppLocale` 列 + `zh-CN Success` / `en-US Success` 分项汇总
  - 环境变量 `RESEARCHKIT_TARGET_LOCALES` 支持单 locale 调试

---

## 验证

### 静态验证

- `tsc --noEmit`：0 errors ✅

### HTTP 冒烟测试（D40-3）

4 种 locale × 2 个页面 = 8 个 case，全部 PASS：

| Locale | Page | Status | Untranslated Keys |
|---|---|---|---|
| zh-CN | / | 200 | 0 |
| zh-CN | /settings | 200 | 0 |
| en-US | / | 200 | 0 |
| en-US | /settings | 200 | 0 |
| ja-JP | / | 200 | 0 |
| ja-JP | /settings | 200 | 0 |
| auto | / | 200 | 0 |
| auto | /settings | 200 | 0 |

### 完整 LLM 回归测试（需用户自行运行）

```powershell
$env:RESEARCHKIT_API_KEY="sk-xxx"
$env:RESEARCHKIT_PROVIDER="deepseek"
$env:RESEARCHKIT_MODEL="deepseek-chat"
npm run test:regression
```

预期：10 fixture × 2 locale = 20 个 case，100% 成功率。

---

## 升级指南

### 用户侧

1. 升级后首次访问，Application Language 默认 `auto`（跟随浏览器）
2. 进入 Settings → General tab 自定义 4 层语言设置
3. 开启 Auto Translate 后，Explain / Chat / Compare 模块的回复会跟随 Application Language
4. 在主页输入论文时，LanguageDetectBanner 会自动检测源语言并建议切换 Output

### 开发者侧

1. 新增 UI 文案时，先在 `locales/zh-CN/*.json` + `locales/en-US/*.json` 添加 key
2. 组件内通过 `const { t } = useI18n()` + `t('namespace.key', { param: value })` 调用
3. 避免 `.map((t, i) => ...)` 等变量遮蔽（应改为 `tool` / `tag` / `prev`）
4. `useCallback` 内使用 `t` 时记得加入依赖数组

---

## 已知限制

- `ja-JP` locale 在 SSR 时 fallback 到 `en-US`（无 `locales/ja-JP/` 目录，运行时 fallback）
- 完整 LLM 流程测试（边界 case：中+英 / 英+中 / AutoTranslate 开关）需用户提供 LLM API Key 自行运行

---

## PR 列表

| PR | Day | Title |
|---|---|---|
| #30 | D37 | feat(i18n): D37 — Settings UI 全面国际化 |
| #31 | D38 | feat(i18n): D38 — 主页 + Agent 模块国际化 |
| #32 | D39 | feat(i18n): D39 — Auto Translate + 智能检测 |
| 本 PR | D40 | feat(i18n): D40 — 回归测试 i18n 扩展 + v2.3.0 Release |

---

## 下一步

v2.3.0 完成后，后续 v2.4 计划：
- `locales/ja-JP/` 目录补齐（消除 SSR fallback）
- LLM 自动检测源语言（替代当前 Unicode 范围统计）
- KC 字段标签跟随 Output Language（当前跟随 Application Language，可能产生混淆）
- 更多 locale（ko-KR / fr-FR / de-DE）

---

**Plan Reference**: `docs/v2.3-i18n-plan.md`
