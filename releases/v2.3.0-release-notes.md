# ResearchKit OS v2.3.0 — Plugin System v2 + i18n + UI 打磨

> **发布日期**：2026-07-22
> **类型**：Minor Release（7 个 Phase / D22-D40 / 14 个 PR）
> **完整路线图**：`docs/v2.3-roadmap.md` + `docs/v2.3-i18n-plan.md`

---

## 核心亮点（7 阶段总览）

| Phase | Day | 模块 | 核心能力 |
|---|---|---|---|
| 1 | D22 | Onchain Export 双模式 | mock/real 双模式重构，6 个可替换接口（TxSigner/IpfsUploader/NonceProvider/GasEstimator/ContractCaller/WalletConnector）|
| 2 | D25-D26 | Engineering 债务 | AgentTimeline 拆分 + ChatUsage 类型化 + cost history server 化 |
| 3 | D27-D29 | Streaming + 持久化 | LiveThoughts token 流式 + KC/Cost History API + localStorage 持久化 |
| 4 | D30 | Smart Suggestion v2 | LLM 驱动的相似 KC 推荐（heuristic fallback）+ server-user-preferences |
| 5 | D31-D33 | Plugin System v2 | PluginManifest + 市场面板 + 批量执行队列（BatchToolbar + 串行队列）|
| 6 | D34-D35 | UI 打磨 | ScrollToTop 浮动按钮（SVG 进度环 + 可拖拽）+ KC 自动滚动 + 拖拽位置约束 |
| 7 | D36-D40 | i18n 完整国际化 | 4 层语言分离架构 + 6 个 namespace + LanguageDetectBanner |

---

## Phase 1 — Onchain Export 双模式（D22）

`core/plugins/onchain-export.ts` + `lib/onchain-{mock,real,modes}.ts` + `types/onchain.ts`

- 抽出 6 个可替换接口：`TxSigner` / `IpfsUploader` / `NonceProvider` / `GasEstimator` / `ContractCaller` / `WalletConnector`
- `lib/onchain-modes.ts` — `resolveOnchainMode(config)` 按 `config.mode` 切换 mock/real
- mock 模式行为完全不变（保留 v2.2.5 demo 行为）
- real 模式预留 D23/D24 真实 SDK 接入点（OKX Agentic Wallet + Pinata + viem）

## Phase 2 — Engineering 债务（D25-D26）

`components/AgentTimeline.tsx` + `lib/usage-collector.ts` + `lib/cost-history.ts`

- AgentTimeline 拆分为 `PipelineChip` + 顶层组合，减少单文件 800+ 行复杂度
- `types/usage.ts` — `ChatUsage` / `AgentUsageSummary` 类型化（替代 `any`）
- `lib/persistence/cost-history-server.ts` — 服务端 cost history 读写

## Phase 3 — Streaming + 持久化（D27-D29）

`components/LiveThoughts.tsx` + `app/api/history/*/route.ts` + `lib/persistence/kc-history-server.ts`

- **LiveThoughts**：SSE `agent_token` 事件 → token 流式展示（Planner / Reflection / Replan 实时输出）
  - ref 累积 + 60ms throttle setState（避免每个 token 触发 re-render）
- **KC History API**：`GET /api/history/kc` 服务端读取 localStorage，支持分页
- **Cost History API**：`GET /api/history/cost` 同上

## Phase 4 — Smart Suggestion v2（D30）

`lib/server-smart-suggestion.ts` + `app/api/research/smart-suggestion/route.ts` + `prompts/smart-suggestion.ts`

- LLM 驱动：把当前 KC + 历史列表发给 LLM，LLM 返回最相似 KC + 推荐理由
- Heuristic fallback：LLM 失败时自动降级到 v1 启发式评分（保证 banner 不漏）
- `lib/server-user-preferences.ts` — 服务端 UserPreferences 读写（cookie 传递）

## Phase 5 — Plugin System v2（D31-D33）

`types/plugin-manifest.ts` + `lib/persistence/plugin-marketplace-server.ts` + `lib/plugin-marketplace.ts` + `components/PluginPanel.tsx`

### D31 — 插件接口 v2

- `types/plugin.ts` 新增 `PluginPermissions`（kcFields / externalApis / network / filesystem / walletSignature）
- `lifecycle` 钩子：`onEnable` / `onDisable` / `onUninstall`
- `category` 字段：`export / publish / sync / analyze`
- `core/plugins/registry.ts` — `triggerLifecycle()` + `listByCategory()`

### D32 — Plugin Marketplace

- `types/plugin-manifest.ts` — 市场条目 schema（id / name / version / author / icon / tags / category / configSchema / permissions / installCount / rating / official）
- `lib/persistence/plugin-marketplace-server.ts` — 服务端 manifest 数据源
  - 3 个内置：`json-download` / `markdown-download` / `onchain-export`
  - 4 个社区 mock：`notion-publish` / `obsidian-publish` / `arxiv-source` / `ipfs-pin`
- `app/api/plugins/{marketplace,install}/route.ts` — GET 市场 + POST 模拟安装
- `lib/plugin-marketplace.ts` — 客户端封装（localStorage 持久化已安装 manifest）

### D33 — 批量执行队列

- `BatchToolbar` 组件：全选 / 清空 / 执行全部 + SVG 进度条（紫色执行中 / 绿色完成 / 绿→红部分失败）
- `PluginCard` 批量模式适配：checkbox + 选中边框 + 执行中高亮
- `executePlugin` 共用函数抽取（单次 + 批量复用）
- `handleBatchExport` 串行队列（依次执行选中插件，汇总成功/失败数）

## Phase 6 — UI 打磨（D34-D35）

`components/ScrollToTop.tsx` + `app/page.tsx`

- **ScrollToTop**：fixed 浮动按钮 + SVG `stroke-dashoffset` 滚动进度环（渐变 indigo→cyan）
- **可拖拽**：mousedown + mousemove 移动位置，移动 < 4px 视为点击（拖拽过不触发回顶）
- **KC 生成后自动滚动**：`scrollIntoView({ behavior: 'smooth' })` 到结果区（demo 友好）
- **LiveThoughts 浮窗移至左下角**：避免与 ScrollToTop 互相遮挡
- **Form submit**：textarea 内 Enter 直接提交，Shift+Enter 换行

## Phase 7 — i18n 完整国际化（D36-D40）

### 4 层语言分离架构

| 层级 | 名称 | 作用 | 可选值 |
|---|---|---|---|
| 1 | **Application Language** | UI 文案 / Help / Tooltip / Preset label | `auto / zh-CN / en-US / ja-JP` |
| 2 | **Output Language** | KC 输出语言 | `auto`（跟随源语言）或显式 `zh-CN / en-US / ja-JP` |
| 3 | **Auto Translate** | Explain / Chat / Compare 回复语言 | `On / Off`（开启时跟随 Application Language） |
| 4 | **Prompt Language** | LLM 内部 prompt 语言 | 锁死 `en-US`（效果最佳，不可改） |

**技术栈**：自建零依赖 `t(key, params, locale)` 系统（< 1KB），6 个 namespace（`home / agent / common / settings / preset / export`），无重型 i18next / react-intl 依赖。

### D36 — i18n 基础设施

- `lib/i18n.ts` — 核心 `t()` 函数，支持 `{param}` 占位符替换
- `lib/locale-types.ts` — `AppLocale` / `ResolvedLocale` 类型
- `components/I18nProvider.tsx` — `useI18n()` hook 返回 `{ appLocale, resolvedLocale, t, setLocale }`
- SSR 行为：服务端渲染 `'auto'` → `'en-US'`，hydration 后切换到 cookie 中的 locale

### D37 — Settings UI 全面国际化（PR #30）

- 5 个 Settings Tab 标签走 i18n
- `components/settings/tabs/GeneralTab.tsx` — 4 层语言设置选择器
- `ProviderTab / PromptTab / CostTab / AboutTab` — 全部本地化
- 新增 60+ locale key 到 `locales/{zh-CN,en-US}/settings.json`

### D38 — 主页 + Agent 模块国际化（PR #31）

**主页 + KC 字段标签**：
- `app/page.tsx` — 全部 UI 文案走 `t()`（`home.*` namespace）
- `lib/ui-labels.ts` — 新增 `getKcFieldLabels(appLocale)`，KC 字段标签跟随 Application Language（而非 Output Language）

**4 个 Agent 组件 i18n 化**：
- `AgentTimeline.tsx`
- `SmartSuggestionBanner.tsx` — `fmtRelativeTime(ts, t)` 接收 t 函数
- `ChatWithKC.tsx` — `generateSuggestions(kc, t)` 重构为接收 t 函数
- `ExplainKC.tsx` — `AUDIENCE_OPTIONS` 改为 `localeKey` + `audienceLabel()` 辅助函数

**其他组件**：
- `CompareTab.tsx` + `PluginPanel.tsx` 全部 i18n 化

### D39 — Auto Translate + 智能检测（PR #32）

**前端 — 输入语言检测**：
- `lib/detect-language.ts` — `detectInputLanguage()` 复用 `lib/locale.ts` 的 `detectLocale`（Unicode 范围统计），返回 `{ detected, sampleSize, counts, confidence }`
- `getLanguageSuggestion()` — 智能建议触发条件：
  - `outputLocale === 'auto'`（用户未显式选 Output）
  - `detected !== resolveAppLocale(appLocale)`（源语言与 UI 语言不同）
  - `detected !== 'other'`（无法判断时不建议）
- `components/LanguageDetectBanner.tsx` — 显示检测结果 + 详情折叠 + 一键切换建议
  - 防抖 300ms，文本 < 20 字符不显示，"忽略" 按本次会话记忆

**后端 — Auto Translate 指令**：
- `lib/server-user-preferences.ts` 新增 `buildAutoTranslateDirective()`
  - `autoTranslate=false` → 返回空字符串（原行为）
  - `autoTranslate=true` → 追加 `Please respond in {appLocale}.` 指令到 system prompt 末尾
- 3 个路由追加 directive：
  - `app/api/research/explain-kc/route.ts`
  - `app/api/research/chat-kc/route.ts`
  - `app/api/research/compare-papers/route.ts`

### D40 — 回归测试 i18n 扩展

- `scripts/regression-test.ts` 扩展：
  - 每篇 fixture 跑两次（`appLocale = outputLocale = zh-CN / en-US`）
  - 通过 cookie 注入 `researchkit-user-preferences`（base64 JSON）
  - `validateKc` 校验按 `expectedOutputLocale` 而非 `fixture.locale`
  - 报告新增 `AppLocale` 列 + `zh-CN Success` / `en-US Success` 分项汇总

---

## 额外修复（v2.3.0 release 前）

- **form submit**：输入框用 `<form onSubmit>` 包裹，生成按钮改 `type="submit"`，mode tabs/loadExample 加 `type="button"` 避免误触
- **版本号同步**：footer i18n + `/api/health` + onchain-export 插件 meta.version 全部统一为 `2.3.0`

---

## 验证

### 静态验证

- `tsc --noEmit`：0 errors ✅

### HTTP 冒烟测试（D40-3）

4 种 locale × 2 个页面 = 8 个 case，全部 PASS：

| Locale | Page | Status | Untranslated Keys |
|---|---|---|---|
| zh-CN | / | 200 | 0 |
| zh-CN | /settings | 200 | 0 |
| en-US | / | 200 | 0 |
| en-US | /settings | 200 | 0 |
| ja-JP | / | 200 | 0 |
| ja-JP | /settings | 200 | 0 |
| auto | / | 200 | 0 |
| auto | /settings | 200 | 0 |

### 完整 LLM 回归测试（需用户自行运行）

```powershell
$env:RESEARCHKIT_API_KEY="sk-xxx"
$env:RESEARCHKIT_PROVIDER="deepseek"
$env:RESEARCHKIT_MODEL="deepseek-chat"
npm run test:regression
```

预期：10 fixture × 2 locale = 20 个 case，100% 成功率。

---

## 升级指南

### 用户侧

1. 升级后首次访问，Application Language 默认 `auto`（跟随浏览器）
2. 进入 Settings → General tab 自定义 4 层语言设置
3. 开启 Auto Translate 后，Explain / Chat / Compare 模块的回复会跟随 Application Language
4. 在主页输入论文时，LanguageDetectBanner 会自动检测源语言并建议切换 Output

### 开发者侧

1. 新增 UI 文案时，先在 `locales/zh-CN/*.json` + `locales/en-US/*.json` 添加 key
2. 组件内通过 `const { t } = useI18n()` + `t('namespace.key', { param: value })` 调用
3. 避免 `.map((t, i) => ...)` 等变量遮蔽（应改为 `tool` / `tag` / `prev`）
4. `useCallback` 内使用 `t` 时记得加入依赖数组

---

## 已知限制

- `ja-JP` locale 在 SSR 时 fallback 到 `en-US`（无 `locales/ja-JP/` 目录，运行时 fallback）
- 完整 LLM 流程测试（边界 case：中+英 / 英+中 / AutoTranslate 开关）需用户提供 LLM API Key 自行运行

---

## PR 列表

| PR | Day | Phase | Title |
|---|---|---|---|
| #34 | D22 | 1 | feat(onchain): D22 — 双模式重构（mock/real 可切换）|
| #35 | D25 | 2 | refactor(agent-timeline): D25 — 拆分 PipelineChip + ChatUsage 类型化 |
| #36 | D26 | 2 | refactor(cost-history): D26 — server 化 + API 路由 |
| #37 | D27 | 3 | feat(live-thoughts): D27 — SSE agent_token 流式 + throttle |
| #38 | D28 | 3 | feat(kc-history): D28 — KC History API + 持久化 |
| #39 | D29 | 3 | feat(cost-history): D29 — Cost History API + 持久化 |
| #40 | D30 | 4 | feat(smart-suggestion): D30 — LLM v2 + heuristic fallback |
| #41 | D31 | 5 | feat(plugin): D31 — Plugin System v2 接口（权限/生命周期/类别）|
| #42 | D32 | 5 | feat(plugin-marketplace): D32 — manifest + 市场 + install API |
| #43 | D33 | 5 | feat(plugin-batch): D33 — 批量执行队列 + BatchToolbar |
| #44 | D34-D35 | 6 | feat(ui-polish): D34-35 — ScrollToTop + 自动滚动 |
| #30 | D37 | 7 | feat(i18n): D37 — Settings UI 全面国际化 |
| #31 | D38 | 7 | feat(i18n): D38 — 主页 + Agent 模块国际化 |
| #32 | D39 | 7 | feat(i18n): D39 — Auto Translate + 智能检测 |
| #33 | D40 | 7 | feat(i18n): D40 — 回归测试 i18n 扩展 |

---

## 下一步

v2.3.0 完成后，后续 v2.4 计划：
- `locales/ja-JP/` 目录补齐（消除 SSR fallback）
- LLM 自动检测源语言（替代当前 Unicode 范围统计）
- KC 字段标签跟随 Output Language（当前跟随 Application Language，可能产生混淆）
- 更多 locale（ko-KR / fr-FR / de-DE）

---

**Plan Reference**: `docs/v2.3-i18n-plan.md`
