# v2.2.6 — Stability Hotfix

**Date**: 2026-07-21
**Branch**: `main` (merged from `feature/v2.2.6-hotfix`)
**Tag**: `v2.2.6`
**Type**: Stability Hotfix — 修复 v2.2.5 发布后发现的 2 个真实小问题,无新功能

> Hotfix-only release. No new features. No prompt text modified.
> Two real issues found in v2.2.5 production verification, both fixed in this
> patch.

---

## Fixed

### Hotfix 1: zh-002 Summary 长度警告误报

**Symptom**: v2.2.5 regression 报告中 zh-002 (TransE) 触发 `Summary too short: 45 chars` 警告。

**Root cause**: Reader prompt 明确要求 `"One-sentence factual summary (subject + verb + object, no fluff)"`,45 chars 是合法的一句话事实摘要(TransE 概念本身就简洁),测试阈值 50 过严导致误报。

**Fix**: `scripts/regression-test.ts` line 314 — summary 长度阈值 50 → 30。真正异常短(< 30 chars)的 summary 仍触发警告。

**Hard constraint respected**: Reader prompt 文本未修改(受 "prompt 文本完全照搬" 硬约束),仅调整测试阈值。

### Hotfix 2: SmartSuggestionBanner 移动端溢出

**Symptom**: SmartSuggestionBanner 原始 minWidth 220(content)+ 140(buttons)+ 32(padding)= 392px,在 320px viewport(老 iPhone SE / 小屏 Android)溢出。

**Fix**: `app/page.tsx` 添加 `.smart-suggestion-wrapper` class + `@media (max-width: 640px)` 规则:

- Banner 切换到 `flex-direction: column`(垂直堆叠)
- 内容区 `min-width: 0`(原 220px)
- 按钮组 `width: 100%` + `justify-content: stretch`
- 按钮 `flex: 1` + `min-height: 44px`(触摸友好)

桌面端布局(>= 641px)保持不变 — banner 仍是水平布局 + 内联按钮。

---

## Verification

| Check | Result |
|---|---|
| `tsc --noEmit` | ✅ 0 errors |
| Browser smoke test 1280x800 | ✅ PASS (桌面布局保持) |
| Browser smoke test 320x568 | ✅ PASS (banner 垂直堆叠,无溢出) |
| `document.documentElement.scrollWidth` | ✅ 320 (无水平溢出) |
| Console errors | ✅ 无新错误引入 |

---

## Files Changed (2)

| File | Change | LOC |
|---|---|---|
| `scripts/regression-test.ts` | summary 阈值 50 → 30 + 注释 | +5/-1 |
| `app/page.tsx` | `.smart-suggestion-wrapper` + `@media` 规则 | +31/-2 |

**Total**: 2 files, +33 insertions, -3 deletions

---

## Hard Constraints Compliance

- ✅ **Prompt 文本完全照搬** — Hotfix 1 仅调整测试阈值,Reader prompt 文本未修改
- ✅ **不 force push v2.2.5** — 新建 v2.2.6-hotfix tag,不覆盖 v2.2.5
- ✅ **不引入新功能** — 仅修复 2 个真实小问题
- ✅ **每个 PR 独立合并** — v2.2.6-hotfix 单独 PR 合并到 main

---

## Migration

No migration needed. v2.2.5 → v2.2.6 is a drop-in replacement.

- `git pull origin main && git checkout v2.2.6` 即可
- 或 `gh release download v2.2.6`
- `npm install` 不需要重跑(无依赖变更)

---

## Next Steps

v2.2.6 是 OKX AI Genesis Hackathon 7/28 07:59 freeze point 前的最后一个 hotfix。后续:

- **7/22-7/27**: 观察 v2.2.6 production stability。如发现新问题,新建 `v2.2.7-hotfix` tag(不 force push v2.2.6)。
- **7/28 07:59**: OKX AI Genesis Hackathon 提交基线为 v2.2.6。
- **7/28+**: v2.3 开发在 ChainHack 期间(7/28-8/5)进行,独立分支。

---

## Links

- [CHANGELOG entry](https://github.com/yuuumc/researchkit/blob/main/docs/CHANGELOG.md#v226--2026-07-21--stability-hotfix)
- [v2.2.5 Quality Release](https://github.com/yuuumc/researchkit/releases/tag/v2.2.5) (previous release)
- [Demo](https://researchkit-mu.vercel.app)
