# v2.0-rc1 — ResearchKit OS Release Candidate 1

**Date**: 2026-07-25
**Branch**: `develop`
**Status**: Release Candidate (frozen at Day 5 of v2.0 refactor)

> 候选版本 — 通过 5 天渐进式重构，架构从「Hackathon Demo」升级为「可扩展 AI Research Agent Platform」。
> 7/28 07:59（OKX 截止）前 develop 冻结，等待最终冒烟决策：合并到 main 发 v2.0 / 回退到 main v1.0。

---

## 架构亮点

### 1. 统一 AgentInterface
所有 Agent 实现统一接口：
```typescript
export interface AgentInterface {
  name: string
  description: string
  capabilities: AgentCapability[]
  execute(ctx: AgentContext): Promise<AgentResult>   // v2.0 统一入口
  handleMessage(message: AgentMessage): Promise<AgentMessage>  // @deprecated v1.0 兼容
  getCapabilities(): AgentCapability[]
}
```

新增 Agent（如 `CriticAgent`）只需：
1. `class CriticAgent implements AgentInterface`
2. 注册到 `AGENTS` 表

`executor` 和 `coordinator` 完全不用改 — 真正的开放-封闭原则。

### 2. 统一 AgentContext
所有 Agent 共享同一份上下文：
```typescript
export interface AgentContext {
  document: { content, language, locale, title?, source?, metadata? }
  workflow: { inputType, requiredSchema, complexity, plan? }
  previous: { reader?, analyzer?, terminology?, knowledgeCard?, recommendation? }
  options: { locale, sourceLocale, languageDirective, detailLevel? }
  promptPatch?: { focus_fields, ignore_fields, extra_instruction }
}
```
未来加 Memory / UserPref / History 不用改接口。

### 3. 模块化目录结构
```
core/
├── agents/{reader,analyzer,terminology,knowledge-builder,recommendation,export}/
│   └── index.ts                    # class XxxAgent implements AgentInterface
├── orchestration/
│   ├── planner.ts                  # runPlanner + fallbackPlan
│   ├── executor.ts                 # AGENTS 注册表 + callAgent + buildAgentContext
│   ├── workflow.ts                 # runReflectionLoop + extractKnowledgeCard
│   └── coordinator.ts              # 薄壳：locale → plan → execute → workflow → export
types/
├── agent.ts                        # AgentInterface + AgentContext + AgentResult
├── knowledge.ts                    # ReaderOutput / AnalyzerOutput / KnowledgeCard ...
├── workflow.ts                     # Plan / Step / Trace / Iteration
└── export.ts                       # ExportOutput
prompts/
├── reader.ts / analyzer.ts / terminology.ts / recommendation.ts / planner.ts
```

---

## 5 天重构路径

| Day | 任务 | PR | 改动 |
|---|---|---|---|
| 1 | 类型系统 + 前端拆分 | [#1](https://github.com/yuuumc/researchkit/pull/1) | types/ 4 文件建立 |
| 2 | Coordinator 拆分 | [#2](https://github.com/yuuumc/researchkit/pull/2) | lib/coordinator.ts 617 → 28 行 re-export |
| 3 | Agent 模块化 | [#3](https://github.com/yuuumc/researchkit/pull/3) | 6 agents 迁移到 core/agents/{name}/ |
| 4 | Prompt 独立 | [#4](https://github.com/yuuumc/researchkit/pull/4) | 5 prompts 抽出独立目录 |
| 5 | Agent Interface | [#5](https://github.com/yuuumc/researchkit/pull/5) | 6 agents class 化 + execute(ctx) 统一入口 |

每个 PR 独立通过 `tsc --noEmit` + 真实 SSE 冒烟测试，确保行为等价于 v1.0。

---

## 冒烟测试验证

### 测试输入
POST Transformer 摘要（Attention Is All You Need）到 `/api/research/multi-agent-stream`

### 验证点（全部命中）
- ✅ 7 stages 全部触发：Document Loaded → Plan Generated → Concepts Extracted → Knowledge Card Built → Reflection Loop → Exports Ready → Done
- ✅ 8 位作者完整：Vaswani / Shazeer / Parmar / Uszkoreit / Jones / Gomez / Kaiser / Polosukhin
- ✅ field = Natural Language Processing
- ✅ difficulty = Intermediate
- ✅ year = 2017
- ✅ language = en, locale = en-US
- ✅ Pipeline 7 agents 全部 `success=true`
- ✅ Reflection 1 轮 satisfied
- ✅ steps_executed=7, steps_succeeded=7
- ✅ 2 个 tool calls 全部成功
- ✅ total_duration_ms = 53854（53.8 秒，与 Day 4 同量级）

---

## Day 1-5 详细改动

### Day 5（10 files, +509/-260）
- `types/agent.ts`: AgentInterface 加 `execute(ctx)`，AgentContext 加 `languageDirective`/`plan`/`recommendation`，AgentResult 默认泛型 `unknown` → `any`
- 6 agents class 化：`export const XxxAgent: Agent` → `export class XxxAgent implements AgentInterface`
- 共享三方法：`execute(ctx)` → `private _run(payload)` → `AgentResult`
- `executor.ts`: AGENTS 改实例；`callAgent(agent, ctx)` 调 `agent.execute(ctx)`；`buildTaskMessage` → `buildAgentContext`
- `coordinator.ts`: 最终 Export 改 AgentContext + `new ExportAgent()`
- `workflow.ts`: KB 重建跟随新返回结构

### Day 4（5 prompts, 609 行）
- 5 个 LLM prompt 抽出到 `prompts/` 独立目录
- Prompt 文本一字不改，仅做位置抽取 + 函数封装

### Day 3（6 agents, 1208 行）
- 6 agents 从 `lib/agents/*.ts` 扁平结构迁移到 `core/agents/{name}/index.ts`
- `lib/agents/*.ts` 改为 re-export shim

### Day 2（4 files, 660 行）
- `lib/coordinator.ts` 拆为 `core/orchestration/{planner,executor,workflow,coordinator}.ts`
- SSE 流与 v1.0 完全等价

### Day 1（4 type files）
- `types/{agent,knowledge,workflow,export}.ts` 统一类型层建立

---

## 技术栈

- **Runtime**: Next.js 14.2.5 + React 18
- **Language**: TypeScript 5.x（strict mode）
- **LLM**: DeepSeek（deepseek-v4-flash）
- **Agent 数量**: 7（Planner / Reader / Analyzer / Terminology / KnowledgeBuilder / Recommendation / Export）
- **架构模式**: Plan-driven + Reflection Loop + Tool Use

---

## 与 v1.0 的兼容性

- ✅ 所有 LLM prompt 文本一字不改
- ✅ 所有 SSE 输出格式与 v1.0 完全一致
- ✅ `lib/agents/*.ts`、`lib/coordinator.ts` 改为 re-export，旧 import 路径无需修改
- ✅ 真实冒烟测试输出与 v1.0 等价（8 作者 / NLP / Intermediate / 2017 / en-US）

---

## 下一步

### 7/28 07:59 决策点
- ✅ 全绿 → 合并 develop → main，打 `v2.0` 正式 tag，发布 GitHub Release
- ❌ 有 bug → 回退到 main（v1.0），用 v1.0 提交 ChainHack；v2.0-rc1 留在 develop 等待修复

### ChainHack 适配期（7/28 → 8/5）
- 主题对齐：Industrial 5.0 — AI × Web3
- 可选：用 v2.0 AgentInterface 快速加 `OnchainExportAgent`，输出知识卡链上 hash 到 X Layer

---

## 关联文档

- [v2-refactor-plan.md](../docs/v2-refactor-plan.md) — 5 天重构计划 + 进度跟踪
- [CHANGELOG.md](../docs/CHANGELOG.md) — 版本日志
- [roadmap.md](../docs/roadmap.md) — 长期规划
- [BRANCHING.md](../docs/BRANCHING.md) — Git 分支策略
- [v1.0-release-notes.md](./v1.0-release-notes.md) — v1.0 基线版本

---

**Tag**: `v2.0-rc1`
**Commit**: 4899869
**Author**: yuuumc
